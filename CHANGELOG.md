# Changelog

## 0.4.0

- 新增 iptables IPv4 转发防火墙与 MASQUERADE 出口 NAT，自动绑定已确认的 WAN/LAN。
- 支持 LAN→WAN 默认策略、按来源/目标 IPv4、TCP/UDP 端口或范围、ICMP 匹配的顺序规则及规则开关、排序。
- 放行已建立连接与返回流量，阻止 WAN 主动连接 LAN；仅管理 Pilot 专用转发/NAT 链，保留本机访问规则。
- 新增预览、90 秒限时确认、手动/超时回滚、失败恢复与重启恢复；启用期间禁止修改 WAN/LAN。
- DNS 页面增加一键保存与未保存提示，修复成功提示消失。

## 0.3.1

- 按单人内网使用方式移除管理员密钥、会话及登录/退出界面。
- WAN/LAN、DHCP 与 DNS 页面和 API 直接可用，保留同源请求校验与网络回滚。

## 0.3.0

- 新增 DHCP 与 DNS 页面、独立服务开关、配置预览和租约列表。
- 实现 Go DNS 本地 A/AAAA 精确覆盖、上游转发、UDP/TCP 支持和基础计数。
- 通过 DNS 功能关闭的独立 dnsmasq 提供 LAN DHCP 地址池、租期、网关与 DNS 下发。
- 服务只绑定已确认的 LAN；拒绝本机上游循环、跨网段地址池、管理接口及过期预览。
- 持久化服务配置与 DHCP 租约；助手崩溃后恢复，服务启用期间禁止直接修改 WAN/LAN。
- 加入真实隔离 LAN/WAN 客户端集成测试，覆盖地址获取、DNS 响应、接口隔离及重启恢复。

## 0.2.0

- 新增 WAN/LAN 页面、管理员密钥登录和接口发现。
- 支持 ifupdown 简单 IPv4 配置：WAN DHCP/静态地址，LAN 静态地址。
- 提供校验、预览、草稿、临时应用、90 秒确认和手动/自动回滚。
- 用持久化事务在网络助手重启后恢复未确认配置；支持保护管理接口。
- Web 服务保持非 root，网络助手仅通过受权限限制的 Unix socket 接受请求。
- 增加认证与网络事务测试，以及隔离网络中的静态地址和真实 DHCP 集成测试。

尚未实现 DHCP 服务器、DNS 管理、NAT、IPv6 配置及已确认接口角色迁移。

## 0.1.0

- 建立 Go + Vue 3 + TypeScript + Vite 项目。
- 实现 Linux CPU、内存、运行时间、系统负载和网卡监控。
- 支持每两秒采样、最近四分钟趋势、暂停刷新及断连提示。
- 提供总览、系统监控和后续功能规划页面。
- 提供 systemd 非 root 运行配置、构建说明及验证记录。

当前仅提供只读监控，尚未包含认证、HTTPS 与网络配置功能。
