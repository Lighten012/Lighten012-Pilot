# v0.1 验证记录

验证环境：Debian 13 x86_64 开发虚拟机；浏览器默认桌面视口及 390px 窄屏。

- Vue TypeScript 类型检查与 Vite 生产构建通过。
- `go test ./...` 与 `go vet ./...` 通过。
- CPU 测试覆盖 guest 计数去重、idle/iowait；内存测试覆盖 MemAvailable 口径及缺失字段；网卡速率测试覆盖计数器重置及无效时间差。
- HTTP 测试覆盖健康检查以及未实现的修改 API 返回 404。
- 在运行服务的非 root DynamicUser 环境读取 `/proc` 与网卡信息成功。
- API 连续采样时间递增，CPU 范围合法、内存数值合理，总内存与 `/proc/meminfo` 一致，网卡地址与系统实际地址一致。
- 浏览器验证总览、系统监控、功能规划、规划详情弹窗、暂停/继续更新及时间范围选择。
- 390px 窄屏 DOM 宽度无横向溢出，页面适配单列与紧凑侧栏。
- 停止服务后手动刷新，出现断连提示并标明旧数据；启动服务后恢复连接。未重启虚拟机，仅验证了应用服务重启。
- 服务已启用开机启动；历史记录仅保存在内存，服务重启后重新积累。

本版本尚未验证高并发、长时间稳定性或公网部署；没有登录认证、HTTPS 或系统修改接口。
