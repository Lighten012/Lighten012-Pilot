# 验证记录

## v0.4.0

- 前端 TypeScript 检查、Vite 生产构建、`go test ./...`、`go vet ./...` 通过。
- 单元测试覆盖端口/协议/地址校验、拒绝过期预览、并发事务、应用失败恢复、超时拒绝确认与回滚、确认配置重启恢复、未确认事务重启回滚、停用恢复原转发开关。
- `tests/firewall_integration.py` 在隔离 LAN/路由器/WAN 网络命名空间验证：真实 TCP/UDP NAT（WAN 端观察到路由器地址）、无 NAT 的路由、来源/目标地址和 TCP 端口阻止、UDP 端口范围、首条规则优先、停用规则、默认阻止、WAN 主动连接 LAN 被拒绝。
- 集成测试验证手动及超时回滚恢复连接、助手崩溃后回滚、确认配置重启恢复、网络修改互锁、过期预览拒绝、INPUT/OUTPUT 与其他链保留、多次应用不重复插入入口、停用恢复原 IPv4 转发值。
- DHCP/DNS 集成回归通过，包括真实地址分配、UDP/TCP 解析、WAN 隔离、进程重启和端口冲突恢复。
- 真实虚拟机的 systemd 助手已验证临时启用 iptables/NAT、IPv4 转发、管理 HTTP 连通、保留 DHCP/DNS 配置，以及回滚恢复。测试后防火墙/NAT 保持关闭，无待确认事务。
- 浏览器验证新入口、规则编辑、非法端口提示、有效预览、未保存提示、待确认倒计时和编辑禁用、回滚后状态恢复；当前窄窗口截图检查通过。

尚未在用户的实际 LAN 客户端验证公网浏览或长期连接。本版仅管理 IPv4 转发与源 NAT；无本机入站防护、IPv6 防火墙、DNAT、持续外部规则漂移检测或连接跟踪清理。

## v0.3.1

- DNS 记录区域新增一键“保存并应用”和未保存提示；修复保存成功提示被配置回读清除的问题。TypeScript 检查和生产构建通过。
- 浏览器真实保存临时记录 `pilot-save-check.invalid → 192.0.2.123`，确认成功提示保留、切换到总览再进入记录仍在；服务器配置文件已持久化，实际 DNS 查询返回该地址。
- 用户重启虚拟机后，API 仍返回保存记录；重新打开浏览器页面正确回读。临时测试记录已删除，用户新增记录保留，磁盘与 API 配置一致，DNS/DHCP 均正常运行。
- 移除管理员密钥与登录，更新相关测试。前端构建、Go 测试和静态检查通过。
- 实际 HTTP 请求不带 Cookie 或凭据，即可读取 WAN/LAN、DHCP/DNS 状态和进行配置预览。
- 修改请求继续检查同源 Origin 和请求头；测试确认正常请求到达网络助手，跨站请求被拒绝。
- 部署取消 systemd 密钥加载依赖，Web 服务与 DHCP/DNS 均正常运行。

## v0.3

- 前端 TypeScript 检查与 Vite 构建、`go test ./...`、`go vet ./...` 通过。
- DNS 单元测试：A/AAAA 本地解析、域名大小写、NODATA 不外泄、不隐式匹配子域名、拒绝多问题及区域传送、真实 UDP 上游截断后 TCP 重试、错误问题拒绝、上游失败。
- DHCP 配置测试：跨网段、包含网关或广播地址、地址池顺序、租约解析；服务配置覆盖受保护接口和本机上游循环拒绝。
- `python3 tests/services_integration.py ./bin/pilot-netd` 使用独立 router/LAN/WAN 网络命名空间：真实 DHCP DORA 获取租约，验证下发网关、DNS、租期；LAN UDP/TCP 查询本地记录；WAN 无 DHCP 和 DNS 响应，包括从 WAN 指向 LAN IP 的查询。
- 上述测试验证助手崩溃后 DNS/DHCP 自动恢复及租约持久化、网络应用互锁、停用 DHCP 后无响应。
- 隔离网络中占用 DNS 端口后，启用请求返回错误且未保存新配置；释放端口后重试成功。
- 用户重新启动开发虚拟机后，确认 DNS/DHCP 按保存配置自动恢复，两个应用服务均为 active。
- `tests/network_integration.py` 通过回归验证原有地址应用、限时回滚及进程恢复。
- 开发虚拟机实际监听确认：Go DNS 的 UDP/TCP 53 和 DHCP UDP 67 均限定 LAN 网卡；实际向 Pilot 发起未配置域名查询，经配置的公共上游成功返回答案。
- 浏览器验证管理员登录、运行状态、记录编辑、有效记录预览和无效地址错误提示；390px 窄屏无横向溢出。临时编辑记录已移除，没有保存示例记录。

尚未覆盖长期租约续租、物理客户端兼容性、满负载、公网暴露和 DNSSEC；本版不提供 DNS 缓存、静态 DHCP 绑定或 NAT。

## v0.2

验证环境：Debian 13 x86_64、ifupdown；桌面浏览器和 390px 窄屏。

- 最新 Vue TypeScript 检查、Vite 生产构建、`go test ./...` 和 `go vet ./...` 通过。
- 单元测试覆盖地址校验、配置解析、拒绝自定义钩子、旧预览拒绝、并发申请拒绝、确认 ID、超时和失败恢复、认证与请求来源校验。
- `python3 tests/network_integration.py ./bin/pilot-netd` 在独立网络命名空间通过：真实地址应用、超时恢复并删除新增配置文件、确认、手动恢复、助手进程崩溃后恢复、WAN 静态地址和默认网关恢复。
- `python3 tests/network_dhcp_integration.py ./bin/pilot-netd` 在独立客户端/网关网络命名空间通过：真实 DHCP 获得租约和默认路由，恢复静态地址，宿主 `/etc/resolv.conf` 内容不变。需 root、iproute2、ifupdown、DHCP 客户端、dnsmasq 二进制；不会启动宿主 DHCP 服务器。
- 开发虚拟机真实 LAN 已完成应用、手动恢复、再次应用并确认；管理 WAN 的地址及原始 interfaces 文件保持不变。
- 用户再次启动虚拟机后，确认 WAN 与已保存 LAN 地址均恢复。未在整机重启期间制造待确认事务；该场景目前验证到助手进程重启恢复。
- 浏览器已验证登录、管理口禁止修改、网段冲突提示、预览、草稿、等待确认倒计时及操作禁用；390px 宽度无横向溢出，控制台无错误。

尚未验证 DHCP 长期续租、长时间负载、多接口复杂拓扑、公网部署、完整路由转发或下游客户端上网。DHCP 服务器和 NAT 尚未实现。

## v0.1

验证环境：Debian 13 x86_64 开发虚拟机；浏览器默认桌面视口及 390px 窄屏。

- Vue TypeScript 类型检查与 Vite 生产构建通过。
- `go test ./...` 与 `go vet ./...` 通过。
- CPU 测试覆盖 guest 计数去重、idle/iowait；内存测试覆盖 MemAvailable 口径及缺失字段；网卡速率测试覆盖计数器重置及无效时间差。
- HTTP 测试覆盖健康检查以及未实现的修改 API 返回 404。
- 在运行服务的非 root DynamicUser 环境读取 `/proc` 与网卡信息成功。
- API 连续采样时间递增，CPU 范围合法、内存数值合理，总内存与 `/proc/meminfo` 一致，网卡地址与系统实际地址一致。
- 浏览器验证总览、系统监控、功能规划、规划详情弹窗、暂停/继续更新及时间范围选择。
- 390px 窄屏 DOM 宽度无横向溢出，页面适配单列与紧凑侧栏。
- 停止服务后手动刷新，出现断连提示并标明旧数据；启动服务后恢复连接。未重启虚拟机，仅验证了应用服务重启。
- 服务已启用开机启动；历史记录仅保存在内存，服务重启后重新积累。

本版本尚未验证高并发、长时间稳定性或公网部署；没有登录认证、HTTPS 或系统修改接口。
